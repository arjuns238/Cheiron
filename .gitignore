assignment-specs.md

# Secrets stay local; the template ships, because CLAUDE.md and the README point at it as
# the record of which variables the service needs.
.env
.env.*
!.env.example

__pycache__/
*.pyc

# Sweep results are regenerated by tests/run_sweep.py and are large.
tests/sweep_results/
